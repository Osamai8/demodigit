
import React from "react";
const CustomInput = ({
    optionArray,
    label,
    name,
    inputType,
    id,
    placeholder,
    onChange,
    require,
    disabledValue,
    onBlur,
    value,
    hiddenValue,
    minValue,
    maxValue,
    defaultValue,
    textColor,
    error,
}) => {
    return (
        <div>

        </div>
    );
}
export default CustomInput;
